function BowlingGame() {
  this.runningScore = 0;
  this.frameNumber = 1;
  this.frameArray = [];
}

// check if theres an open frame
//   if frameArray.count < frameNumber
//     var frame = new Frame(frameNumber)
//     frame.takeTurn
//   else 
//   return frame
//   frame.takeTurn


//   until this.frameNumber === 10
  
//   var frame = new Frame (frameNumber);
//   frame.takeTurn;
//   frame
//   frame.updateFrameCount;



  BowlingGame.prototype.checkFrame = function(frameNumber) {
    
  };



  BowlingGame.prototype.newFrame = function(frameNumber) {
    
  };


  BowlingGame.prototype.calcRollCount = function( pinsHit ) {
    
  };
  
  BowlingGame.prototype.calcFrameCount = function( pinsHit ) {

  };

  BowlingGame.prototype.calcFrameScore = function( pinsHit ) { 
    maxScore = 30;
    rollCount = 3; 
    if (pinsHit === 10) {
      
    }

  };


